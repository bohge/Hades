-- Generated by lua-capnproto 0.1.3-1 on Wed Mar  8 18:59:36 2017
-- https://github.com/cloudflare/lua-capnproto.git


local ffi = require "ffi"
local capnp = require "capnp"
local bit = require "bit"

local ceil              = math.ceil
local write_struct_field= capnp.write_struct_field
local read_struct_field = capnp.read_struct_field
local read_text         = capnp.read_text
local write_text        = capnp.write_text
local get_enum_val      = capnp.get_enum_val
local get_enum_name     = capnp.get_enum_name
local get_data_off      = capnp.get_data_off
local write_listp_buf   = capnp.write_listp_buf
local write_structp_buf = capnp.write_structp_buf
local write_structp     = capnp.write_structp
local read_struct_buf   = capnp.read_struct_buf
local read_listp_struct = capnp.read_listp_struct
local read_list_data    = capnp.read_list_data
local write_list        = capnp.write_list
local write_list_data   = capnp.write_list_data
local ffi_new           = ffi.new
local ffi_string        = ffi.string
local ffi_cast          = ffi.cast
local ffi_copy          = ffi.copy
local ffi_fill          = ffi.fill
local ffi_typeof        = ffi.typeof
local band, bor, bxor = bit.band, bit.bor, bit.bxor

local pint8    = ffi_typeof("int8_t *")
local pint16   = ffi_typeof("int16_t *")
local pint32   = ffi_typeof("int32_t *")
local pint64   = ffi_typeof("int64_t *")
local puint8   = ffi_typeof("uint8_t *")
local puint16  = ffi_typeof("uint16_t *")
local puint32  = ffi_typeof("uint32_t *")
local puint64  = ffi_typeof("uint64_t *")
local pbool    = ffi_typeof("uint8_t *")
local pfloat32 = ffi_typeof("float *")
local pfloat64 = ffi_typeof("double *")


local ok, new_tab = pcall(require, "table.new")

if not ok then
    new_tab = function (narr, nrec) return {} end
end

local round8 = function(size)
    return ceil(size / 8) * 8
end

local str_buf
local default_segment_size = 4096

local function get_str_buf(size)
    if size > default_segment_size then
        return ffi_new("char[?]", size)
    end

    if not str_buf then
        str_buf = ffi_new("char[?]", default_segment_size)
    end
    return str_buf
end

-- Estimated from #nodes, not accurate
local _M = new_tab(0, 4)


_M.ClusterMessageID = {
    ["CMID_INCOME_CONNECT"] = 0,
    ["CMID_TENSOR_TRANSFER"] = 1,

}

_M.ClusterMessageIDStr = {
    [0] = "CMID_INCOME_CONNECT",
    [1] = "CMID_TENSOR_TRANSFER",

}

_M.IncomeConnect = {
    id = "16453251865671956325",
    displayName = "cluster.capnp:IncomeConnect",
    dataWordCount = 1,
    pointerCount = 1,
    discriminantCount = 0,
    discriminantOffset = 0,
    field_count = 2,

    fields = {
        { name = "ipadress", default = "", ["type"] = "text" },
        { name = "port", default = 0, ["type"] = "int32" },
    },

    calc_size_struct = function(data)
        local size = 16
        local value
        -- text
        value = data["ipadress"]
        if type(value) == "string" then
            -- size 1, including trailing NULL
            size = size + round8(#value + 1)
        end
        return size
    end,

    calc_size = function(data)
        local size = 16 -- header + root struct pointer
        return size + _M.IncomeConnect.calc_size_struct(data)
    end,

    flat_serialize = function(data, p32, pos)
        pos = pos and pos or 16 -- struct size in bytes
        local start = pos
        local dscrm
        local value

        value = data["ipadress"]
        if type(value) == "string" then
            local data_off = get_data_off(_M.IncomeConnect, 0, pos)

            local len = #value + 1
            write_listp_buf(p32, _M.IncomeConnect, 0, 2, len, data_off)

            ffi_copy(p32 + pos / 4, value)
            pos = pos + round8(len)
        end

        value = data["port"]
        local data_type = type(value)
        if (data_type == "number"
                or data_type == "boolean" ) then

            write_struct_field(p32, value, "int32", 32, 0, 0)
        end
        return pos - start + 16
    end,

    serialize = function(data, p8, size)
        if not p8 then
            size = _M.IncomeConnect.calc_size(data)

            p8 = get_str_buf(size)
        end
        ffi_fill(p8, size)
        local p32 = ffi_cast(puint32, p8)

        -- Because needed size has been calculated, only 1 segment is needed
        p32[0] = 0
        p32[1] = (size - 8) / 8

        -- skip header
        write_structp(p32 + 2, _M.IncomeConnect, 0)

        -- skip header & struct pointer
        _M.IncomeConnect.flat_serialize(data, p32 + 4)

        return ffi_string(p8, size)
    end,

    parse_struct_data = function(p32, data_word_count, pointer_count, header,
            tab)

        local s = tab

        -- text
        local off, size, num = read_listp_struct(p32, header, _M.IncomeConnect, 0)
        if off and num then
            -- dataWordCount + offset + pointerSize + off
            local p8 = ffi_cast(pint8, p32 + (1 + 0 + 1 + off) * 2)
            s["ipadress"] = ffi_string(p8, num - 1)
        else
            s["ipadress"] = nil
        end

        s["port"] = read_struct_field(p32, "int32", 32, 0, 0)

        return s
    end,

    parse = function(bin, tab)
        if #bin < 16 then
            return nil, "message too short"
        end

        local header = new_tab(0, 4)
        local p32 = ffi_cast(puint32, bin)
        header.base = p32

        local nsegs = p32[0] + 1
        header.seg_sizes = {}
        for i=1, nsegs do
            header.seg_sizes[i] = p32[i]
        end
        local pos = round8(4 + nsegs * 4)
        header.header_size = pos / 8
        p32 = p32 + pos / 4

        if not tab then
            tab = new_tab(0, 8)
        end
        local off, dw, pw = read_struct_buf(p32, header)
        if off and dw and pw then
            return _M.IncomeConnect.parse_struct_data(p32 + 2 + off * 2, dw, pw,
                    header, tab)
        else
            return nil
        end
    end,

}

_M.TensorTransfer = {
    id = "13226489199645849232",
    displayName = "cluster.capnp:TensorTransfer",
    dataWordCount = 1,
    pointerCount = 2,
    discriminantCount = 0,
    discriminantOffset = 0,
    field_count = 3,

    fields = {
        { name = "tensortype", default = "", ["type"] = "text" },
        { name = "buffersize", default = 0, ["type"] = "uint32" },
        { name = "dimension", default = "opaque pointer", ["type"] = "list" },
    },

    calc_size_struct = function(data)
        local size = 24
        local value
        -- text
        value = data["tensortype"]
        if type(value) == "string" then
            -- size 1, including trailing NULL
            size = size + round8(#value + 1)
        end
        -- list
        if data["dimension"] and type(data["dimension"]) == "table" then
            -- num * acutal size
            size = size + round8(#data["dimension"] * 4)
        end
        return size
    end,

    calc_size = function(data)
        local size = 16 -- header + root struct pointer
        return size + _M.TensorTransfer.calc_size_struct(data)
    end,

    flat_serialize = function(data, p32, pos)
        pos = pos and pos or 24 -- struct size in bytes
        local start = pos
        local dscrm
        local value

        value = data["tensortype"]
        if type(value) == "string" then
            local data_off = get_data_off(_M.TensorTransfer, 0, pos)

            local len = #value + 1
            write_listp_buf(p32, _M.TensorTransfer, 0, 2, len, data_off)

            ffi_copy(p32 + pos / 4, value)
            pos = pos + round8(len)
        end

        value = data["buffersize"]
        local data_type = type(value)
        if (data_type == "number"
                or data_type == "boolean" ) then

            write_struct_field(p32, value, "uint32", 32, 0, 0)
        end

        value = data["dimension"]
        if type(value) == "table" then
            local data_off = get_data_off(_M.TensorTransfer, 1, pos)
            pos = pos + write_list(p32 + _M.TensorTransfer.dataWordCount * 2 + 1 * 2,
                    value, (data_off + 1) * 8, "list", "uint32")
        end
        return pos - start + 24
    end,

    serialize = function(data, p8, size)
        if not p8 then
            size = _M.TensorTransfer.calc_size(data)

            p8 = get_str_buf(size)
        end
        ffi_fill(p8, size)
        local p32 = ffi_cast(puint32, p8)

        -- Because needed size has been calculated, only 1 segment is needed
        p32[0] = 0
        p32[1] = (size - 8) / 8

        -- skip header
        write_structp(p32 + 2, _M.TensorTransfer, 0)

        -- skip header & struct pointer
        _M.TensorTransfer.flat_serialize(data, p32 + 4)

        return ffi_string(p8, size)
    end,

    parse_struct_data = function(p32, data_word_count, pointer_count, header,
            tab)

        local s = tab

        -- text
        local off, size, num = read_listp_struct(p32, header, _M.TensorTransfer, 0)
        if off and num then
            -- dataWordCount + offset + pointerSize + off
            local p8 = ffi_cast(pint8, p32 + (1 + 0 + 1 + off) * 2)
            s["tensortype"] = ffi_string(p8, num - 1)
        else
            s["tensortype"] = nil
        end

        s["buffersize"] = read_struct_field(p32, "uint32", 32, 0, 0)

        -- list
        local off, size, num = read_listp_struct(p32, header, _M.TensorTransfer, 1)
        if off and num then
            -- dataWordCount + offset + pointerSize + off
            s["dimension"] = read_list_data(p32 + (1 + 1 + 1 + off) * 2, header,
                    num, "uint32")
        else
            s["dimension"] = nil
        end

        return s
    end,

    parse = function(bin, tab)
        if #bin < 16 then
            return nil, "message too short"
        end

        local header = new_tab(0, 4)
        local p32 = ffi_cast(puint32, bin)
        header.base = p32

        local nsegs = p32[0] + 1
        header.seg_sizes = {}
        for i=1, nsegs do
            header.seg_sizes[i] = p32[i]
        end
        local pos = round8(4 + nsegs * 4)
        header.header_size = pos / 8
        p32 = p32 + pos / 4

        if not tab then
            tab = new_tab(0, 8)
        end
        local off, dw, pw = read_struct_buf(p32, header)
        if off and dw and pw then
            return _M.TensorTransfer.parse_struct_data(p32 + 2 + off * 2, dw, pw,
                    header, tab)
        else
            return nil
        end
    end,

}

return _M
